#!/bin/bash
# ABOUTME: Example shell script template

set -euo pipefail

# Script configuration
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

main() {
    echo "Example script - replace with actual implementation"
}

main "$@"
