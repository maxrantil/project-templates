# ABOUTME: Blocks direct pushes to master branch (only allows PR merges)
name: Protect Master Branch

on:
  push:
    branches:
      - master
      - main

jobs:
  block-direct-push:
    permissions:
      contents: read
      pull-requests: read
    uses: maxrantil/.github/.github/workflows/protect-master-reusable.yml@master
